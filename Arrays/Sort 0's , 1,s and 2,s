#include <bits/stdc++.h>
using namespace std;

int main()
{
	int testcase = 0;
	cin>>testcase;
	while(testcase--)
	{
	    int sze;
	    cin>>sze;
	    int arr[sze];
	    for (int i=0; i<sze;i++)
	    cin>>arr[i];

	    sort(arr, arr+sze);
	    for (int i=0; i<sze;i++)
	    {
	        cout<<arr[i]<<"  ";
	    }
	    cout<<endl;
	}

	return 0;
}
